{
    "mcpServers": {
        "esp32-telegram": {
            "command": "python",
            "args": [
                "servers/esp32_server/esp32_server.py"
            ],
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    }
}